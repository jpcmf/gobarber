const INITIAL_STATE = {
  token: 123,
};

export default function auth(state = INITIAL_STATE, action) {
  switch (action.type) {
    // case value:
    //   break;

    default:
      return state;
  }
}
